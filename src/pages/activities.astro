---
import Layout from "../layouts/Page.astro";
import ReserveDatePicker from "../components/ReserveDatePicker.astro";
---

<Layout
  seo={{
    title: "Activities & Rentals",
    description:
      "Reserve our pavilion & kitchen, grab pool passes, or rent kayaks at FourWardCast Family Campground on Bull Shoals Lake. Fun for the whole family.",
  }}
>
  <!-- ═══════════════ HERO ═══════════════ -->
  <section class="act-hero">
    <div class="act-hero__topo" aria-hidden="true">
      <svg viewBox="0 0 1440 320" preserveAspectRatio="none">
        <path d="M0,160 C180,80 360,240 540,160 C720,80 900,240 1080,160 C1200,100 1320,200 1440,160 L1440,320 L0,320 Z" fill="var(--theme-bg)" opacity="0.3"/>
        <path d="M0,200 C200,140 400,260 600,200 C800,140 1000,260 1200,200 C1320,160 1380,230 1440,200 L1440,320 L0,320 Z" fill="var(--theme-bg)" opacity="0.5"/>
        <path d="M0,260 C240,220 480,300 720,260 C960,220 1200,300 1440,260 L1440,320 L0,320 Z" fill="var(--theme-bg)" opacity="0.8"/>
      </svg>
    </div>
    <div class="act-hero__content reveal">
      <p class="act-hero__eyebrow">FourWardCast Family Campground</p>
      <h1 class="act-hero__title">Activities &<br/><span class="act-hero__accent">Rentals</span></h1>
      <p class="act-hero__sub">Make the most of your stay with lakeside kayaking, poolside relaxation, and a fully equipped pavilion for your next gathering.</p>
    </div>
  </section>

  <!-- ═══════════════ CATEGORY TABS ═══════════════ -->
  <section class="act-tabs-wrap">
    <nav class="act-tabs reveal" aria-label="Activity type">
      <button class="act-tab is-active" data-tab="pavilion" type="button">
        <span class="act-tab__icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M3 21h18"/><path d="M4 21V10l8-6 8 6v11"/><path d="M9 21v-4h6v4"/><path d="M3 10h18"/></svg>
        </span>
        <span class="act-tab__label">Pavilion & Kitchen</span>
      </button>
      <button class="act-tab" data-tab="pool" type="button">
        <span class="act-tab__icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M2 20c2-1 4 1 6 0s4-1 6 0 4 1 6 0"/><path d="M2 16c2-1 4 1 6 0s4-1 6 0 4 1 6 0"/><rect x="6" y="3" width="4" height="13" rx="2"/><rect x="14" y="3" width="4" height="13" rx="2"/></svg>
        </span>
        <span class="act-tab__label">Pool Passes</span>
      </button>
      <button class="act-tab" data-tab="kayak" type="button">
        <span class="act-tab__icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M2 12c2.5-3 5.5-5 10-5s7.5 2 10 5"/><path d="M2 12c2.5 3 5.5 5 10 5s7.5-2 10-5"/><line x1="12" y1="2" x2="12" y2="22"/><line x1="4" y1="6" x2="8" y2="10"/><line x1="20" y1="6" x2="16" y2="10"/></svg>
        </span>
        <span class="act-tab__label">Kayak Rentals</span>
      </button>
      <div class="act-tab__slider" aria-hidden="true"></div>
    </nav>
  </section>

  <!-- ═══════════════ TAB PANELS ═══════════════ -->
  <div class="act-panels">

    <!-- ──── PAVILION & KITCHEN ──── -->
    <article class="act-panel is-active" data-panel="pavilion">
      <div class="panel-grid">
        <!-- Gallery -->
        <div class="panel-gallery reveal">
          <div class="gallery-main">
            <div class="gallery-placeholder" data-category="pavilion">
              <div class="gallery-placeholder__gradient"></div>
              <svg class="gallery-placeholder__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1" stroke-linecap="round"><path d="M3 21h18"/><path d="M4 21V10l8-6 8 6v11"/><path d="M9 21v-4h6v4"/><path d="M3 10h18"/></svg>
              <span class="gallery-placeholder__text">Pavilion Photo</span>
            </div>
            <img class="gallery-img" src={`${import.meta.env.BASE_URL}assets/images/activities/pavilion-1.jpg`} alt="Covered pavilion with full kitchen at FourWardCast" loading="lazy" style="display:none" onload="this.style.display='block';this.previousElementSibling.style.display='none'"/>
          </div>
          <div class="gallery-thumbs">
            <div class="gallery-thumb">
              <div class="gallery-placeholder gallery-placeholder--sm" data-category="pavilion">
                <div class="gallery-placeholder__gradient"></div>
              </div>
              <img class="gallery-img" src={`${import.meta.env.BASE_URL}assets/images/activities/pavilion-2.jpg`} alt="Kitchen area" loading="lazy" style="display:none" onload="this.style.display='block';this.previousElementSibling.style.display='none'"/>
            </div>
            <div class="gallery-thumb">
              <div class="gallery-placeholder gallery-placeholder--sm" data-category="pavilion">
                <div class="gallery-placeholder__gradient"></div>
              </div>
              <img class="gallery-img" src={`${import.meta.env.BASE_URL}assets/images/activities/pavilion-3.jpg`} alt="Pavilion seating" loading="lazy" style="display:none" onload="this.style.display='block';this.previousElementSibling.style.display='none'"/>
            </div>
            <div class="gallery-thumb">
              <div class="gallery-placeholder gallery-placeholder--sm" data-category="pavilion">
                <div class="gallery-placeholder__gradient"></div>
              </div>
              <img class="gallery-img" src={`${import.meta.env.BASE_URL}assets/images/activities/pavilion-4.jpg`} alt="Pavilion evening event" loading="lazy" style="display:none" onload="this.style.display='block';this.previousElementSibling.style.display='none'"/>
            </div>
          </div>
        </div>

        <!-- Details -->
        <div class="panel-details">
          <div class="panel-header reveal">
            <h2 class="panel-title">Pavilion & Kitchen</h2>
            <p class="panel-tagline">A covered outdoor space with a fully equipped kitchen. Perfect for reunions, cookouts, birthday parties, and group gatherings.</p>
          </div>

          <!-- Amenities -->
          <div class="amenities-grid reveal">
            <div class="amenity-chip">
              <span class="amenity-chip__dot" style="background:var(--theme-surface-1)"></span>
              Covered Pavilion
            </div>
            <div class="amenity-chip">
              <span class="amenity-chip__dot" style="background:var(--theme-surface-2)"></span>
              Full Kitchen
            </div>
            <div class="amenity-chip">
              <span class="amenity-chip__dot" style="background:var(--theme-surface-1)"></span>
              Tables & Chairs
            </div>
            <div class="amenity-chip">
              <span class="amenity-chip__dot" style="background:var(--theme-surface-2)"></span>
              Electricity
            </div>
            <div class="amenity-chip">
              <span class="amenity-chip__dot" style="background:var(--theme-surface-1)"></span>
              Internet Access
            </div>
            <div class="amenity-chip">
              <span class="amenity-chip__dot" style="background:var(--theme-surface-2)"></span>
              Restrooms Nearby
            </div>
            <div class="amenity-chip">
              <span class="amenity-chip__dot" style="background:var(--theme-surface-1)"></span>
              Parking
            </div>
            <div class="amenity-chip">
              <span class="amenity-chip__dot" style="background:var(--theme-surface-2)"></span>
              Scenic Setting
            </div>
          </div>

          <!-- Pricing Cards -->
          <div class="pricing-row reveal">
            <div class="price-card">
              <span class="price-card__period">Hourly</span>
              <span class="price-card__amount"><sup>$</sup>50</span>
              <span class="price-card__unit">2 hr minimum</span>
            </div>
            <div class="price-card price-card--featured">
              <span class="price-card__badge">Most Popular</span>
              <span class="price-card__period">Half Day</span>
              <span class="price-card__amount"><sup>$</sup>175</span>
              <span class="price-card__unit">up to 4 hrs</span>
              <span class="price-card__save">Save $25</span>
            </div>
            <div class="price-card">
              <span class="price-card__period">Full Day</span>
              <span class="price-card__amount"><sup>$</sup>350</span>
              <span class="price-card__unit">up to 8 hrs</span>
              <span class="price-card__save">Save $50</span>
            </div>
          </div>

          <p class="pricing-note reveal">Capacity up to 50 guests. A deposit of $50 is required at booking. Campers receive 10% off.</p>

          <div class="picker-section reveal">
            <p class="picker-label">Select your dates & reserve</p>
            <ReserveDatePicker
              parkSlug="fourwardcast-flippin-ar"
              d="5f5b6ce3-89da-4cb3-b8d4-6a2688b9c0d5"
              buttonText="Reserve Now"
            />
          </div>
        </div>
      </div>
    </article>

    <!-- ──── POOL PASSES ──── -->
    <article class="act-panel" data-panel="pool">
      <div class="panel-grid">
        <div class="panel-gallery reveal">
          <div class="gallery-main">
            <div class="gallery-placeholder" data-category="pool">
              <div class="gallery-placeholder__gradient"></div>
              <svg class="gallery-placeholder__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1" stroke-linecap="round"><path d="M2 20c2-1 4 1 6 0s4-1 6 0 4 1 6 0"/><path d="M2 16c2-1 4 1 6 0s4-1 6 0 4 1 6 0"/><rect x="6" y="3" width="4" height="13" rx="2"/><rect x="14" y="3" width="4" height="13" rx="2"/></svg>
              <span class="gallery-placeholder__text">Pool Photo</span>
            </div>
            <img class="gallery-img" src={`${import.meta.env.BASE_URL}assets/images/activities/pool-1.jpg`} alt="Swimming pool at FourWardCast" loading="lazy" style="display:none" onload="this.style.display='block';this.previousElementSibling.style.display='none'"/>
          </div>
          <div class="gallery-thumbs">
            <div class="gallery-thumb">
              <div class="gallery-placeholder gallery-placeholder--sm" data-category="pool">
                <div class="gallery-placeholder__gradient"></div>
              </div>
              <img class="gallery-img" src={`${import.meta.env.BASE_URL}assets/images/activities/pool-2.jpg`} alt="Pool lounge area" loading="lazy" style="display:none" onload="this.style.display='block';this.previousElementSibling.style.display='none'"/>
            </div>
            <div class="gallery-thumb">
              <div class="gallery-placeholder gallery-placeholder--sm" data-category="pool">
                <div class="gallery-placeholder__gradient"></div>
              </div>
              <img class="gallery-img" src={`${import.meta.env.BASE_URL}assets/images/activities/pool-3.jpg`} alt="Kids swimming" loading="lazy" style="display:none" onload="this.style.display='block';this.previousElementSibling.style.display='none'"/>
            </div>
            <div class="gallery-thumb">
              <div class="gallery-placeholder gallery-placeholder--sm" data-category="pool">
                <div class="gallery-placeholder__gradient"></div>
              </div>
              <img class="gallery-img" src={`${import.meta.env.BASE_URL}assets/images/activities/pool-4.jpg`} alt="Poolside view" loading="lazy" style="display:none" onload="this.style.display='block';this.previousElementSibling.style.display='none'"/>
            </div>
          </div>
        </div>

        <div class="panel-details">
          <div class="panel-header reveal">
            <h2 class="panel-title">Pool Passes</h2>
            <p class="panel-tagline">Cool off after a day on the lake. Our pool is open seasonally with passes available for campers and day visitors alike.</p>
          </div>

          <div class="amenities-grid reveal">
            <div class="amenity-chip">
              <span class="amenity-chip__dot" style="background:var(--theme-surface-1)"></span>
              Seasonal Pool
            </div>
            <div class="amenity-chip">
              <span class="amenity-chip__dot" style="background:var(--theme-surface-2)"></span>
              Lounge Chairs
            </div>
            <div class="amenity-chip">
              <span class="amenity-chip__dot" style="background:var(--theme-surface-1)"></span>
              Shallow End for Kids
            </div>
            <div class="amenity-chip">
              <span class="amenity-chip__dot" style="background:var(--theme-surface-2)"></span>
              Camp Store Nearby
            </div>
            <div class="amenity-chip">
              <span class="amenity-chip__dot" style="background:var(--theme-surface-1)"></span>
              Shaded Seating
            </div>
            <div class="amenity-chip">
              <span class="amenity-chip__dot" style="background:var(--theme-surface-2)"></span>
              Restrooms
            </div>
          </div>

          <div class="pricing-row reveal">
            <div class="price-card">
              <span class="price-card__period">Day Pass</span>
              <span class="price-card__amount"><sup>$</sup>5</span>
              <span class="price-card__unit">/ person</span>
            </div>
            <div class="price-card price-card--featured">
              <span class="price-card__badge">Most Popular</span>
              <span class="price-card__period">Weekly</span>
              <span class="price-card__amount"><sup>$</sup>20</span>
              <span class="price-card__unit">/ person</span>
              <span class="price-card__save">Save $15</span>
            </div>
            <div class="price-card">
              <span class="price-card__period">Season</span>
              <span class="price-card__amount"><sup>$</sup>75</span>
              <span class="price-card__unit">/ person</span>
            </div>
          </div>

          <p class="pricing-note reveal">Children under 3 swim free. Registered campers receive complimentary day passes during their stay. <br>Pool hours: 9 AM – 8 PM. <br> Season: March 1st - October 31st</p>

          <div class="picker-section reveal">
            <p class="picker-label">Select your dates & reserve</p>
            <ReserveDatePicker
              parkSlug="fourwardcast-flippin-ar"
              d="5f5b6ce3-89da-4cb3-b8d4-6a2688b9c0d5"
              buttonText="Get Passes"
            />
          </div>
        </div>
      </div>
    </article>

    <!-- ──── KAYAK RENTALS ──── -->
    <article class="act-panel" data-panel="kayak">
      <div class="panel-grid">
        <div class="panel-gallery reveal">
          <div class="gallery-main">
            <div class="gallery-placeholder" data-category="kayak">
              <div class="gallery-placeholder__gradient"></div>
              <svg class="gallery-placeholder__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1" stroke-linecap="round"><path d="M2 12c2.5-3 5.5-5 10-5s7.5 2 10 5"/><path d="M2 12c2.5 3 5.5 5 10 5s7.5-2 10-5"/><line x1="12" y1="2" x2="12" y2="22"/></svg>
              <span class="gallery-placeholder__text">Kayak Photo</span>
            </div>
            <img class="gallery-img" src={`${import.meta.env.BASE_URL}assets/images/activities/kayak-1.jpg`} alt="Kayaking on Bull Shoals Lake" loading="lazy" style="display:none" onload="this.style.display='block';this.previousElementSibling.style.display='none'"/>
          </div>
          <div class="gallery-thumbs">
            <div class="gallery-thumb">
              <div class="gallery-placeholder gallery-placeholder--sm" data-category="kayak">
                <div class="gallery-placeholder__gradient"></div>
              </div>
              <img class="gallery-img" src={`${import.meta.env.BASE_URL}assets/images/activities/kayak-2.jpg`} alt="Kayak launch area" loading="lazy" style="display:none" onload="this.style.display='block';this.previousElementSibling.style.display='none'"/>
            </div>
            <div class="gallery-thumb">
              <div class="gallery-placeholder gallery-placeholder--sm" data-category="kayak">
                <div class="gallery-placeholder__gradient"></div>
              </div>
              <img class="gallery-img" src={`${import.meta.env.BASE_URL}assets/images/activities/kayak-3.jpg`} alt="kayak on lake" loading="lazy" style="display:none" onload="this.style.display='block';this.previousElementSibling.style.display='none'"/>
            </div>
            <div class="gallery-thumb">
              <div class="gallery-placeholder gallery-placeholder--sm" data-category="kayak">
                <div class="gallery-placeholder__gradient"></div>
              </div>
              <img class="gallery-img" src={`${import.meta.env.BASE_URL}assets/images/activities/kayak-4.jpg`} alt="Scenic lake kayaking" loading="lazy" style="display:none" onload="this.style.display='block';this.previousElementSibling.style.display='none'"/>
            </div>
          </div>
        </div>

        <div class="panel-details">
          <div class="panel-header reveal">
            <h2 class="panel-title">Kayak Rentals</h2>
            <p class="panel-tagline">Explore Bull Shoals Lake at your own pace.</p>
          </div>

          <div class="amenities-grid reveal">
            <div class="amenity-chip">
              <span class="amenity-chip__dot" style="background:var(--theme-surface-1)"></span>
              Single Kayaks
            </div>
            <div class="amenity-chip">
              <span class="amenity-chip__dot" style="background:var(--theme-surface-2)"></span>
              Shuttle Service
            </div>
            <div class="amenity-chip">
              <span class="amenity-chip__dot" style="background:var(--theme-surface-1)"></span>
              Life Jackets Included
            </div>
            <div class="amenity-chip">
              <span class="amenity-chip__dot" style="background:var(--theme-surface-2)"></span>
              Paddles Included
            </div>
            <div class="amenity-chip">
              <span class="amenity-chip__dot" style="background:var(--theme-surface-1)"></span>
              Launch Area Access
            </div>
            <div class="amenity-chip">
              <span class="amenity-chip__dot" style="background:var(--theme-surface-2)"></span>
              No Experience Needed
            </div>
          </div>

          <div class="pricing-row reveal">
            <div class="price-card">
              <span class="price-card__period">Hourly</span>
              <span class="price-card__amount"><sup>$</sup>15</span>
              <span class="price-card__unit">/ kayak</span>
            </div>
            <div class="price-card price-card--featured">
              <span class="price-card__badge">Best Value</span>
              <span class="price-card__period">Half Day</span>
              <span class="price-card__amount"><sup>$</sup>40</span>
              <span class="price-card__unit">/ kayak</span>
              <span class="price-card__save">Save $20</span>
            </div>
            <div class="price-card">
              <span class="price-card__period">Full Day</span>
              <span class="price-card__amount"><sup>$</sup>60</span>
              <span class="price-card__unit">/ kayak</span>
            </div>
          </div>

          <p class="pricing-note reveal">Registered campers receive 10% off all rentals. Available seasonally.</p>

          <div class="picker-section reveal">
            <p class="picker-label">Select your dates & reserve</p>
            <ReserveDatePicker
              parkSlug="fourwardcast-flippin-ar"
              d="5f5b6ce3-89da-4cb3-b8d4-6a2688b9c0d5"
              buttonText="Book Kayaks"
            />
          </div>
        </div>
      </div>
    </article>

  </div>

  <!-- ═══════════════ CONTACT CTA ═══════════════ -->
  <section class="act-cta reveal">
    <div class="act-cta__topo" aria-hidden="true">
      <svg viewBox="0 0 1440 200" preserveAspectRatio="none">
        <path d="M0,0 L0,40 C240,80 480,20 720,60 C960,100 1200,30 1440,60 L1440,0 Z" fill="var(--theme-bg)" opacity="0.5"/>
      </svg>
    </div>
    <div class="act-cta__inner">
      <h2 class="act-cta__title">Questions about activities?</h2>
      <p class="act-cta__text">We're happy to help plan your visit. Give us a call or send an email.</p>
      <div class="act-cta__actions">
        <a class="act-cta__btn" href="tel:+13273245000">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" width="20" height="20"><path d="M22 16.92v3a2 2 0 01-2.18 2 19.79 19.79 0 01-8.63-3.07 19.5 19.5 0 01-6-6 19.79 19.79 0 01-3.07-8.67A2 2 0 014.11 2h3a2 2 0 012 1.72c.127.96.361 1.903.7 2.81a2 2 0 01-.45 2.11L8.09 9.91a16 16 0 006 6l1.27-1.27a2 2 0 012.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0122 16.92z"/></svg>
          (327) 324-5000
        </a>
        <a class="act-cta__btn act-cta__btn--secondary" href="mailto:reservations@fourwardcast.com">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" width="20" height="20"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/><polyline points="22,6 12,13 2,6"/></svg>
          Email Us
        </a>
      </div>
    </div>
  </section>
</Layout>

<!-- ═══════════════ CLIENT SCRIPT ═══════════════ -->
<script is:inline>
(function() {
  /* ── Tab Switching ── */
  const tabs = document.querySelectorAll('.act-tab');
  const panels = document.querySelectorAll('.act-panel');
  const slider = document.querySelector('.act-tab__slider');

  function positionSlider(tab) {
    if (!slider || !tab) return;
    slider.style.width = tab.offsetWidth + 'px';
    slider.style.transform = 'translateX(' + tab.offsetLeft + 'px)';
  }

  function switchTab(targetId) {
    tabs.forEach(function(t) { t.classList.remove('is-active'); });
    panels.forEach(function(p) {
      if (p.dataset.panel === targetId) {
        p.classList.add('is-active');
        /* Re-trigger reveal animations inside new panel */
        p.querySelectorAll('.reveal').forEach(function(el) {
          el.classList.remove('is-visible');
          void el.offsetWidth; /* force reflow */
          el.classList.add('is-visible');
        });
      } else {
        p.classList.remove('is-active');
      }
    });
    var activeTab = document.querySelector('.act-tab[data-tab="' + targetId + '"]');
    if (activeTab) {
      activeTab.classList.add('is-active');
      positionSlider(activeTab);
    }
  }

  tabs.forEach(function(tab) {
    tab.addEventListener('click', function() {
      switchTab(tab.dataset.tab);
      /* Scroll panels into view smoothly */
      var panelsWrap = document.querySelector('.act-panels');
      if (panelsWrap) {
        panelsWrap.scrollIntoView({ behavior: 'smooth', block: 'start' });
      }
    });
  });

  /* Position slider on load and resize */
  function initSlider() {
    var active = document.querySelector('.act-tab.is-active');
    if (active) positionSlider(active);
  }
  initSlider();
  window.addEventListener('resize', initSlider);

  /* ── Scroll-triggered reveal ── */
  var prefersReduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

  if (!prefersReduced && 'IntersectionObserver' in window) {
    var observer = new IntersectionObserver(function(entries) {
      entries.forEach(function(entry) {
        if (entry.isIntersecting) {
          entry.target.classList.add('is-visible');
        }
      });
    }, { threshold: 0.12, rootMargin: '0px 0px -40px 0px' });

    document.querySelectorAll('.reveal').forEach(function(el) {
      observer.observe(el);
    });
  } else {
    document.querySelectorAll('.reveal').forEach(function(el) {
      el.classList.add('is-visible');
    });
  }
})();
</script>

<style>
  /* ════════════════════════════════════════
     REVEAL ANIMATION BASE
     ════════════════════════════════════════ */
  .reveal {
    opacity: 0;
    transform: translateY(24px);
    transition: opacity 0.7s cubic-bezier(0.22, 1, 0.36, 1),
                transform 0.7s cubic-bezier(0.22, 1, 0.36, 1);
  }
  .reveal.is-visible {
    opacity: 1;
    transform: translateY(0);
  }

  /* Stagger children */
  .amenities-grid.is-visible .amenity-chip,
  .pricing-row.is-visible .price-card {
    animation: chipIn 0.5s cubic-bezier(0.22, 1, 0.36, 1) both;
  }
  .amenities-grid.is-visible .amenity-chip:nth-child(1) { animation-delay: 0.04s; }
  .amenities-grid.is-visible .amenity-chip:nth-child(2) { animation-delay: 0.08s; }
  .amenities-grid.is-visible .amenity-chip:nth-child(3) { animation-delay: 0.12s; }
  .amenities-grid.is-visible .amenity-chip:nth-child(4) { animation-delay: 0.16s; }
  .amenities-grid.is-visible .amenity-chip:nth-child(5) { animation-delay: 0.20s; }
  .amenities-grid.is-visible .amenity-chip:nth-child(6) { animation-delay: 0.24s; }
  .amenities-grid.is-visible .amenity-chip:nth-child(7) { animation-delay: 0.28s; }
  .amenities-grid.is-visible .amenity-chip:nth-child(8) { animation-delay: 0.32s; }

  .pricing-row.is-visible .price-card:nth-child(1) { animation-delay: 0.06s; }
  .pricing-row.is-visible .price-card:nth-child(2) { animation-delay: 0.14s; }
  .pricing-row.is-visible .price-card:nth-child(3) { animation-delay: 0.22s; }

  @keyframes chipIn {
    from { opacity: 0; transform: translateY(12px) scale(0.95); }
    to   { opacity: 1; transform: translateY(0) scale(1); }
  }

  /* ════════════════════════════════════════
     HERO
     ════════════════════════════════════════ */
  .act-hero {
    position: relative;
    padding: clamp(4rem, 10vw, 8rem) clamp(1.5rem, 4vw, 3rem) clamp(3rem, 6vw, 5rem);
    background: var(--theme-primary);
    color: var(--theme-on-primary);
    overflow: hidden;
    text-align: center;
  }
  .act-hero__topo {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    line-height: 0;
  }
  .act-hero__topo svg {
    width: 100%;
    height: auto;
    display: block;
  }
  .act-hero__content {
    position: relative;
    z-index: 1;
    max-width: 680px;
    margin: 0 auto;
  }
  .act-hero__eyebrow {
    font-family: var(--theme-font-family-sans);
    text-transform: uppercase;
    letter-spacing: 0.18em;
    font-size: 0.8rem;
    opacity: 0.7;
    margin-bottom: 1rem;
  }
  .act-hero__title {
    font-family: var(--theme-font-family-serif);
    font-size: clamp(2.4rem, 5.5vw, 4rem);
    font-weight: 700;
    line-height: 1.1;
    margin: 0 0 1.2rem;
  }
  .act-hero__accent {
    color: var(--theme-surface-1);
  }
  .act-hero__sub {
    font-size: clamp(1rem, 1.6vw, 1.15rem);
    opacity: 0.85;
    max-width: 520px;
    margin: 0 auto;
    line-height: 1.6;
  }

  /* ════════════════════════════════════════
     TABS
     ════════════════════════════════════════ */
  .act-tabs-wrap {
    display: flex;
    justify-content: center;
    padding: 2.5rem 1rem 1rem;
  }
  .act-tabs {
    position: relative;
    display: inline-flex;
    gap: 0;
    background: rgba(81, 69, 38, 0.08);
    border-radius: 999px;
    padding: 5px;
  }
  .act-tab {
    position: relative;
    z-index: 1;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1.4rem;
    border: none;
    background: none;
    cursor: pointer;
    font-family: var(--theme-font-family-sans);
    font-size: 0.95rem;
    font-weight: 600;
    color: var(--theme-on-bg);
    border-radius: 999px;
    transition: color 0.35s ease;
    white-space: nowrap;
  }
  .act-tab.is-active {
    color: var(--theme-on-primary);
  }
  .act-tab__icon {
    width: 22px;
    height: 22px;
    display: flex;
    align-items: center;
    flex-shrink: 0;
  }
  .act-tab__icon svg {
    width: 100%;
    height: 100%;
  }
  .act-tab__slider {
    position: absolute;
    top: 5px;
    left: 0;
    height: calc(100% - 10px);
    background: var(--theme-primary);
    border-radius: 999px;
    transition: transform 0.4s cubic-bezier(0.22, 1, 0.36, 1),
                width 0.4s cubic-bezier(0.22, 1, 0.36, 1);
    z-index: 0;
    box-shadow: 0 2px 12px rgba(81, 69, 38, 0.25);
  }

  /* ════════════════════════════════════════
     PANELS
     ════════════════════════════════════════ */
  .act-panels {
    position: relative;
    z-index: 10;
    max-width: var(--container-max-width);
    margin: 0 auto;
    padding: 1rem clamp(1rem, 3vw, 2rem) 2rem;
    scroll-margin-top: 1rem;
  }
  .act-panel {
    display: none;
  }
  .act-panel.is-active {
    display: block;
    animation: panelIn 0.55s cubic-bezier(0.22, 1, 0.36, 1) both;
  }
  @keyframes panelIn {
    from { opacity: 0; transform: translateY(16px); }
    to   { opacity: 1; transform: translateY(0); }
  }
  .panel-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: clamp(1.5rem, 3vw, 3rem);
    align-items: start;
    margin-top: 1.5rem;
  }

  /* ════════════════════════════════════════
     GALLERY & PLACEHOLDERS
     ════════════════════════════════════════ */
  .gallery-main {
    border-radius: 20px;
    overflow: hidden;
    aspect-ratio: 4 / 3;
    position: relative;
  }
  .gallery-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }
  .gallery-thumbs {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 0.65rem;
    margin-top: 0.65rem;
  }
  .gallery-thumb {
    border-radius: 12px;
    overflow: hidden;
    aspect-ratio: 4 / 3;
    position: relative;
  }

  /* Placeholder treatments */
  .gallery-placeholder {
    width: 100%;
    height: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
    position: relative;
    overflow: hidden;
  }
  .gallery-placeholder__gradient {
    position: absolute;
    inset: 0;
    opacity: 0.9;
  }
  /* Pavilion = warm terracotta */
  [data-category="pavilion"] .gallery-placeholder__gradient {
    background: linear-gradient(135deg, #7a6248 0%, #9b7d5a 40%, #c4a070 100%);
  }
  /* Pool = cool blue */
  [data-category="pool"] .gallery-placeholder__gradient {
    background: linear-gradient(135deg, #3b6b8a 0%, #5a8faa 40%, #7db8d4 100%);
  }
  /* Kayak = lake green-blue */
  [data-category="kayak"] .gallery-placeholder__gradient {
    background: linear-gradient(135deg, #4a7a6a 0%, #6a9a82 40%, #8cc0a0 100%);
  }
  .gallery-placeholder__icon {
    position: relative;
    z-index: 1;
    width: 64px;
    height: 64px;
    color: rgba(255,255,255,0.5);
  }
  .gallery-placeholder--sm .gallery-placeholder__icon {
    width: 28px;
    height: 28px;
  }
  .gallery-placeholder__text {
    position: relative;
    z-index: 1;
    font-family: var(--theme-font-family-sans);
    font-size: 0.85rem;
    font-weight: 600;
    color: rgba(255,255,255,0.5);
    text-transform: uppercase;
    letter-spacing: 0.1em;
  }

  /* Subtle animated grain on placeholders */
  .gallery-placeholder::after {
    content: '';
    position: absolute;
    inset: 0;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.08'/%3E%3C/svg%3E");
    background-size: 180px;
    pointer-events: none;
    z-index: 1;
  }

  /* Topographic contour lines on the main placeholder */
  .gallery-main .gallery-placeholder::before {
    content: '';
    position: absolute;
    inset: 0;
    z-index: 1;
    pointer-events: none;
    opacity: 0.12;
    background-image:
      repeating-radial-gradient(circle at 30% 60%, transparent 0, transparent 38px, rgba(255,255,255,0.4) 39px, transparent 40px),
      repeating-radial-gradient(circle at 70% 35%, transparent 0, transparent 52px, rgba(255,255,255,0.3) 53px, transparent 54px);
  }

  /* ════════════════════════════════════════
     PANEL DETAILS
     ════════════════════════════════════════ */
  .panel-header {
    margin-bottom: 1.5rem;
  }
  .panel-title {
    font-family: var(--theme-font-family-serif);
    font-size: clamp(1.8rem, 3vw, 2.4rem);
    font-weight: 700;
    margin: 0 0 0.5rem;
    color: var(--theme-on-bg);
  }
  .panel-tagline {
    font-size: 1.05rem;
    opacity: 0.8;
    line-height: 1.5;
    margin: 0;
  }

  /* ════════════════════════════════════════
     AMENITY CHIPS
     ════════════════════════════════════════ */
  .amenities-grid {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-bottom: 2rem;
  }
  .amenity-chip {
    display: inline-flex;
    align-items: center;
    gap: 0.45rem;
    padding: 0.5rem 0.9rem;
    background: rgba(81, 69, 38, 0.06);
    border-radius: 999px;
    font-family: var(--theme-font-family-sans);
    font-size: 0.85rem;
    font-weight: 500;
    color: var(--theme-on-bg);
    border: 1px solid rgba(81, 69, 38, 0.1);
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }
  .amenity-chip:hover {
    transform: translateY(-1px);
    box-shadow: 0 3px 8px rgba(81, 69, 38, 0.1);
  }
  .amenity-chip__dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    flex-shrink: 0;
  }

  /* ════════════════════════════════════════
     PRICING CARDS
     ════════════════════════════════════════ */
  .pricing-row {
    display: flex;
    gap: 0.85rem;
    margin-bottom: 1rem;
    flex-wrap: wrap;
  }
  .price-card {
    flex: 1 1 140px;
    min-width: 130px;
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 1.4rem 1rem 1.2rem;
    background: #fff;
    border-radius: 18px;
    border: 1.5px solid rgba(81, 69, 38, 0.1);
    text-align: center;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }
  .price-card:hover {
    transform: translateY(-3px);
    box-shadow: 0 8px 24px rgba(81, 69, 38, 0.1);
  }
  .price-card--featured {
    background: var(--theme-primary);
    color: var(--theme-on-primary);
    border-color: var(--theme-primary);
    box-shadow: 0 4px 20px rgba(81, 69, 38, 0.2);
    transform: scale(1.04);
  }
  .price-card--featured:hover {
    transform: scale(1.04) translateY(-3px);
    box-shadow: 0 8px 28px rgba(81, 69, 38, 0.25);
  }
  .price-card__badge {
    position: absolute;
    top: -10px;
    left: 50%;
    transform: translateX(-50%);
    padding: 0.2rem 0.75rem;
    font-size: 0.65rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    background: var(--theme-surface-1);
    color: #fff;
    border-radius: 999px;
    white-space: nowrap;
  }
  .price-card__period {
    font-family: var(--theme-font-family-sans);
    font-size: 0.8rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    opacity: 0.7;
    margin-bottom: 0.3rem;
  }
  .price-card__amount {
    font-family: var(--theme-font-family-serif);
    font-size: 2.2rem;
    font-weight: 700;
    line-height: 1;
  }
  .price-card__amount sup {
    font-size: 0.5em;
    position: relative;
    top: -0.4em;
  }
  .price-card__unit {
    font-size: 0.78rem;
    opacity: 0.6;
    margin-top: 0.15rem;
  }
  .price-card__save {
    display: inline-block;
    margin-top: 0.5rem;
    font-size: 0.72rem;
    font-weight: 700;
    padding: 0.15rem 0.5rem;
    border-radius: 999px;
    background: rgba(255,255,255,0.2);
  }
  .price-card:not(.price-card--featured) .price-card__save {
    background: var(--theme-surface-2);
    color: var(--theme-on-surface-2);
  }

  .pricing-note {
    font-size: 0.82rem;
    opacity: 0.6;
    line-height: 1.5;
    margin: 0 0 1.5rem;
  }

  /* ════════════════════════════════════════
     PICKER SECTION
     ════════════════════════════════════════ */
  .picker-section {
    padding-top: 0.5rem;
  }
  .picker-label {
    font-family: var(--theme-font-family-sans);
    font-weight: 600;
    font-size: 0.9rem;
    margin-bottom: 0.6rem;
    color: var(--theme-on-bg);
  }

  /* ════════════════════════════════════════
     CONTACT CTA
     ════════════════════════════════════════ */
  .act-cta {
    position: relative;
    background: var(--theme-primary);
    color: var(--theme-on-primary);
    text-align: center;
    padding: clamp(4rem, 8vw, 6rem) clamp(1.5rem, 4vw, 3rem) clamp(3rem, 6vw, 4rem);
    padding-bottom: calc(clamp(3rem, 6vw, 4rem) + var(--theme-shape-radius));
    margin-top: 3rem;
    margin-bottom: calc(-1 * var(--theme-shape-radius));
    overflow: hidden;
  }
  .act-cta__topo {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    line-height: 0;
  }
  .act-cta__topo svg {
    width: 100%;
    height: auto;
    display: block;
  }
  .act-cta__inner {
    position: relative;
    z-index: 1;
    max-width: 520px;
    margin: 0 auto;
  }
  .act-cta__title {
    font-family: var(--theme-font-family-serif);
    font-size: clamp(1.6rem, 3.5vw, 2.2rem);
    font-weight: 700;
    margin: 0 0 0.75rem;
  }
  .act-cta__text {
    opacity: 0.8;
    margin: 0 0 1.5rem;
    line-height: 1.5;
  }
  .act-cta__actions {
    display: flex;
    justify-content: center;
    gap: 0.85rem;
    flex-wrap: wrap;
  }
  .act-cta__btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.85rem 1.6rem;
    border-radius: var(--theme-button-border-radius);
    text-decoration: none;
    font-family: var(--theme-font-family-sans);
    font-weight: 700;
    font-size: 0.95rem;
    background: var(--theme-surface-1);
    color: #fff;
    transition: transform var(--theme-transition), filter var(--theme-transition);
  }
  .act-cta__btn:hover {
    transform: translateY(-2px);
    filter: brightness(1.08);
  }
  .act-cta__btn--secondary {
    background: rgba(255,255,255,0.15);
    backdrop-filter: blur(4px);
    border: 1.5px solid rgba(255,255,255,0.3);
  }

  /* ════════════════════════════════════════
     RESPONSIVE
     ════════════════════════════════════════ */
  @media (max-width: 900px) {
    .panel-grid {
      grid-template-columns: 1fr;
    }
    .panel-gallery {
      max-width: 560px;
    }
  }
  @media (max-width: 600px) {
    .act-tabs-wrap {
      padding: 2rem 0.5rem 1rem;
    }
    .act-tabs {
      max-width: 100%;
    }
    .act-tab__icon {
      display: none;
    }
    .act-tab__label {
      font-size: 0.82rem;
    }
    .act-tab {
      padding: 0.65rem 0.9rem;
      gap: 0;
    }
    .act-panels {
      padding: 1rem 0.75rem 2rem;
    }
    .pricing-row {
      gap: 0.6rem;
    }
    .price-card {
      padding: 1.2rem 0.75rem 1rem;
    }
    .price-card__amount {
      font-size: 1.8rem;
    }
    .price-card--featured {
      transform: scale(1.02);
    }
    .price-card--featured:hover {
      transform: scale(1.02) translateY(-3px);
    }
  }

  /* ════════════════════════════════════════
     REDUCED MOTION
     ════════════════════════════════════════ */
  @media (prefers-reduced-motion: reduce) {
    .reveal { opacity: 1; transform: none; transition: none; }
    .act-panel.is-active { animation: none; }
    .amenities-grid.is-visible .amenity-chip,
    .pricing-row.is-visible .price-card { animation: none; }
    .act-tab__slider { transition: none; }
  }
</style>
